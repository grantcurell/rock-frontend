---

pip install virtualenv virtualenvwrapper
pip install --upgrade pip
export WORKON_HOME=~/Envs
source /usr/bin/virtualenvwrapper.sh
mkvirtualenv my-venv
workon my-venv
pip install Flask

...

- Need to make sure there is enough storage left over for everything else
- Need to make sure at least 2 Ceph drives are selected 
